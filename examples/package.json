{
  "name": "flow-module-examples",
  "version": "1.3.0",
  "description": "Flow Module Examples",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "flow build",
    "lint": "flow lint",
    "format": "flow format",
    "test": "flow test",
    "package": "flow package",
    "publish": "flow publish-module"
  },
  "dependencies": {
    "@hahnpro/flow-sdk": "^4.0.0"
  },
  "devDependencies": {
    "@hahnpro/flow-cli": "^2.4.0"
  },
  "prettier": {
    "arrowParens": "always",
    "printWidth": 140,
    "singleQuote": true,
    "trailingComma": "all",
    "overrides": [
      {
        "files": "*.ts",
        "options": {
          "parser": "typescript"
        }
      }
    ]
  }
}
